# Asset Pipeline

Use this folder for licensed photoreal media:

- `master/`: original source files (PNG/JPEG)
- `web/`: optimized runtime assets (WebP)

Track every asset in `data/assets-manifest.json` with creator, source URL, and license.
